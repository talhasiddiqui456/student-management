//class StudentManagementSystem {
//     private students: Student[] = [];
//     private courses: Course[] = [];

//     // Method to add a new student
//     addStudent(student: Student): void {
//         this.students.push(student);
//         console.log(`Student ${student['name']} added.`);
//     }

//     // Method to add a new course
//     addCourse(course: Course): void {
//         this.courses.push(course);
//         console.log(`Course ${course.getCourseName()} added.`);
//     }

//     // Method to find a student by ID
//     findStudent(studentId: string): Student | undefined {
//         return this.students.find(student => student['studentId'] === studentId);
//     }

//     // Method to find a course by ID
//     findCourse(courseId: string): Course | undefined {
//         return this.courses.find(course => course.getCourseId() === courseId);
//     }
// }
